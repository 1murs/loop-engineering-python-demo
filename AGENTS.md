# Agent Instructions

You are working on a small Python project.

Main goal:
- Follow docs/SPEC.md
- Do not change tests unless the specification is wrong
- Keep the solution simple
- After every code change, run pytest
- If tests fail, read the error and fix the implementation
- Stop only when all tests pass

Project commands:

Install dependencies:
pip install pytest

Run tests:
pytest

Main source file:
src/cart.py

Test file:
tests/test_cart.py